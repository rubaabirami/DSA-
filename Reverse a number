#include <iostream>
#include <cmath>
using namespace std;

// Time Complexity: O(log10n)
// Space Complexity: O(1)

int main() {
    int i,reversed=0;
    cout << "Enter num = ";
    cin >> i;
while(i>0){
    reversed=(reversed*10)+i%10;
    i=i/10;
}
    cout<<reversed<<endl;

    return 0;
}
